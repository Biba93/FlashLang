<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.github.biba.flashlang"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:name=".FlashLangApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_fl"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_fl"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.Day">
        <activity
            android:name=".ui.activity.SplashScreenActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.AuthActivity"/>
        <activity
            android:name=".ui.activity.MainActivity"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="stateHidden"/>
        <activity android:name=".ui.activity.GameActivity"/>
    </application>

</manifest>
